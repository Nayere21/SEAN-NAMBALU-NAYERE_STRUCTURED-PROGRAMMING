#include <stdio.h>
#include <string.h>

  int main(){
  char name[100];
  int length;


  printf("Enter a string(like your name):" );
  scanf("%s", name);


  printf("You entered: %s\n", name);

  length= strlen(name);

  printf("length of string: %d\n", length);

  return 0;
  }
