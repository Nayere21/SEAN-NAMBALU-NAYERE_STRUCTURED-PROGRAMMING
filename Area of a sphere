#include <stdio.h>

int main() {
    float radius;
    float area;
    float pi = 3.14;

        printf("Enter radius of the sphere: ");
    scanf("%f", &radius);

    area = 4 * pi * radius * radius;

    printf("Area of the sphere = %.2f\n", area);

    return 0;
}
